import styled from 'styled-components';
import colors from '../../../config/colors';

export default styled.h1`
  font-size: 1.15rem;
  font-weight: 600;
  color: ${colors.black};
  letter-spacing: -1px;
`;
